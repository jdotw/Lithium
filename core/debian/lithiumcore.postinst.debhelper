# Automatically added by dh_installinit
if [ -x "/etc/init.d/lithiumcore" ]; then
	update-rc.d lithiumcore defaults >/dev/null
	if [ -x "`which invoke-rc.d 2>/dev/null`" ]; then
		invoke-rc.d lithiumcore start || exit $?
	else
		/etc/init.d/lithiumcore start || exit $?
	fi
fi
# End automatically added section
